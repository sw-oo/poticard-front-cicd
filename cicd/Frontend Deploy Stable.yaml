apiVersion: apps/v1
kind: Deployment
metadata:
  name: poticard-frontend-stable
  labels:
    type: frontend
    version: stable
spec:
  replicas: 1
  selector:
    matchLabels:
      type: frontend
      version: stable
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        type: frontend
        version: stable
    spec:
      containers:
      - name: frontend
        image: tahcy/potifront:0.1
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "100m"
            memory: "128Mi"